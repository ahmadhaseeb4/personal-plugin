name: My Plugin Deploy Action
on:
  push:
    tag:
      - "*"
      
jobs:
  tag:
    name: LOLO Job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: WordPress Plugin Deploy
        uses: ahmadhaseeb4/plugin-deployer@stable
        env:
          URL: "https://james-bond.unfuddle.com/svn/james-bond_af/"
          SVN_USERNAME: "muhammad007"
          SVN_PASSWORD: "12345678aA"