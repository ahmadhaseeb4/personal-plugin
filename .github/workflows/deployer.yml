name: My Plugin Deploy Action
on:
  push:
    branches:
      - master

jobs:
  tag:
    name: My Job
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - name: WordPress Plugin Deploy
        uses: ahmadhaseeb4/deep-fried-deployer@master
        env:
          URL: "https://james-not-bond.unfuddle.com/svn/james-not-bond_one/"
          SVN_USERNAME: "muhammad007"
          SVN_PASSWORD: "12345678aA"
          hellO: "hello"